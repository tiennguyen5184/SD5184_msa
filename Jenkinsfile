pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
               script {
                sh 'whoami'
                // docker ps
               }
            }
        }
    }
}
